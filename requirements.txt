# Direct dependencies only -- transitive ones (CUDA runtimes, keras, tokenizers,
# safetensors, ...) are resolved by pip from the packages below.
# Tested with Python 3.11 - 3.13.

# Numerics / imaging
numpy>=2.0,<3
opencv-python>=4.10
pillow>=11.0
matplotlib>=3.9
pandas>=2.2
scikit-learn>=1.5
tqdm>=4.66

# U-Net de-noising model (UDM). TensorFlow >= 2.16 ships Keras 3, which only
# saves/loads models in the `.keras` format used by these scripts.
tensorflow>=2.18,<3

# TrOCR fine-tuning and inference.
# transformers >= 4.46 is required: `Seq2SeqTrainingArguments.eval_strategy` and
# `Trainer(processing_class=...)` replaced the older `evaluation_strategy` /
# `tokenizer` arguments.
torch>=2.4
transformers>=4.46,<6
accelerate>=1.0

# Character Error Rate. Used directly instead of the removed
# `datasets.load_metric("cer")`.
jiwer>=3.0

# Extraction of the .rar datasets (needs `unrar`, `7z` or `bsdtar` on the PATH).
patool>=3.0
